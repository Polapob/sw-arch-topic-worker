version: '3'
networks:
  rabbitMQ:
    name: topic_rabbitMQ
    external: true

  backend:
    name: topic_backend
    external: true

services:
  worker:
    container_name: comment-worker
    build: .
    networks:
      - rabbitMQ
      - backend
  